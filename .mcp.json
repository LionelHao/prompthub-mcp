{
  "mcpServers": {
    "prompthub": {
      "command": "npx",
      "args": ["-y", "github:LionelHao/prompthub-mcp"],
      "env": {
        "PROMPTHUB_TOKEN": "${PROMPTHUB_TOKEN}"
      }
    }
  }
}
